[project]
name = "research-record"
version = "1.0.0"
description = "A local research record desktop app."
requires-python = ">=3.10"
dependencies = [
  "PySide6==6.7.3",
]

[tool.pyinstaller]
name = "科研记录"
